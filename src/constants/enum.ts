export enum GenderType {
    MALE = 'male',
    FEMALE = 'female',
    OTHER = 'other'
}

export enum TokenType {
    AccessToken,
    RefreshToken
}

export enum VerifyEmailType {
    RESISTER,
    FORGOT_PASSWORD
}

export enum MediaType {
    Image,
    Video,
    Audio,
}
